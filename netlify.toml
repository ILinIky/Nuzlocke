# netlify.toml
[build]
  publish   = "."
  functions = "netlify/functions"

# optional: hübsche /api/*-Route
[[redirects]]
  from = "/api/*"
  to   = "/.netlify/functions/:splat"
  status = 200
  force = true
